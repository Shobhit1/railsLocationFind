<table class="table table-bordered table-hover">
  <thead>
  <tr>
    <th>Address</th>
    <th>Latitude</th>
    <th>Longitude</th>
    <th></th>
  </tr>
  </thead>
  <tbody>
  <% @lots.each do |lot| %>
  <!--<% lot_details = lot.geocode %>-->
      <tr>
        <td><%= lot.address %></td>
        <td><%= lot.latitude %></td>
        <td><%= lot.longitude %></td>
        <!--<td><%= link_to "Detail", lot %></td>-->
        <!--<td><%= link_to "Edit", edit_lot_path(lot) %></td>-->
      </tr>
  <% end %>
  </tbody>
</table>